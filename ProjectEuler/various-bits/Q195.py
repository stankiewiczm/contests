from numpy import *import time;rLIM = 1053779;   LIM = sqrt(12)*rLIM;    iLIM = int(LIM);NUM = 0;          t0 = time.time();       a = 1;while a < LIM:              # a < c < b,   and b-c = k    k  = 0;    if (a%1000 == 0):        print a, NUM;    while (k < (a-1)/2) and (a+k < iLIM):        k += 1;        if (a*a-k*k)%(a-2*k) == 0:            NUM += 1    a += 1;#            b = (a*a-k*k)/(a-2*k);#            c = b-k;    print rLIM, NUM, time.time() - t0;